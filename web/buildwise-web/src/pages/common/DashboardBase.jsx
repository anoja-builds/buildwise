import { Card, PageHeader, StatusBadge } from '../../components/shared'
import './common.css'
const summaries = [['Pending Items', '12', '3 added this week', 'var(--color-warning-700)'], ['Active Workflows', '8', 'Across the workspace', 'var(--color-primary-700)'], ['Attention Required', '3', 'Needs team review', 'var(--color-danger-700)'], ['Completed Today', '19', 'Updated moments ago', 'var(--color-success-700)']]
export default function DashboardBase() { return <div className="stack"><PageHeader title="Dashboard" description="A neutral overview of current workspace activity."/><div className="grid grid--4">{summaries.map(([label, value, note, color]) => <Card key={label} className="summary-card" style={{ '--summary-color': color }}><div className="summary-card__label">{label}</div><div className="summary-card__value">{value}</div><div className="summary-card__note">{note}</div></Card>)}</div><div className="grid grid--2"><Card title="Recent Activity" subtitle="Example updates from across the workspace"><ul className="activity-list">{['A new item was created', 'A record moved to review', 'Team notes were updated'].map((item, index) => <li className="activity-item" key={item}><span className="activity-dot"/><div><p>{item}</p><span className="activity-time">{index + 1} hour{index ? 's' : ''} ago</span></div></li>)}</ul></Card><Card title="Workflow Overview" subtitle="Illustrative progress only">{[['In progress', 65], ['In review', 42], ['Completed', 82]].map(([label, value]) => <div className="progress-row" key={label}><div className="progress-row__meta"><span>{label}</span><StatusBadge status={label === 'Completed' ? 'success' : 'info'}>{value}%</StatusBadge></div><div className="progress"><div className="progress__value" style={{ width: `${value}%` }}/></div></div>)}</Card></div></div> }
