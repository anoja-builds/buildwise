import './AppLayout.css'
const navigation = ['Dashboard', 'Material Requests', 'Suppliers', 'Quotations', 'Procurement', 'Purchase Orders', 'Deliveries', 'Quality Inspections', 'Non-Conformances', 'AI Workflows', 'Reports', 'Users & Roles']
export default function AppLayout({ children, breadcrumb = 'Workspace / Overview', activeItem = 'Dashboard', onNavigate }) {
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand__mark">BW</div><div><div className="brand__name">BuildWise</div><div className="brand__tagline">Project operations</div></div></div><nav className="nav" aria-label="Main navigation">{navigation.map((item) => <button key={item} type="button" className={`nav-item ${item === activeItem ? 'nav-item--active' : ''}`} onClick={() => onNavigate?.(item)}>{item}</button>)}</nav></aside><div className="app-main"><header className="topbar"><div><span className="mobile-brand">BuildWise</span><span className="breadcrumb">{breadcrumb}</span></div><div className="topbar__actions"><button className="icon-button" aria-label="Notifications">♢</button><div className="user"><div className="avatar">JD</div><div className="user__meta"><div className="user__name">Jordan Doe</div><div className="user__role">Team member</div></div></div></div></header><main className="content">{children}</main></div></div>
}
