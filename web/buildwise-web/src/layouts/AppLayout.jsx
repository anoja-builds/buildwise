import './AppLayout.css'
const navigation = ['Dashboard', 'Material Requests', 'Suppliers', 'Quotations', 'Procurement', 'Purchase Orders', 'Deliveries', 'Quality Inspections', 'Non-Conformances', 'AI Workflows', 'Reports', 'Users & Roles']
const initialsOf = (name) => name ? name.split(' ').filter(Boolean).slice(0, 2).map((p) => p[0].toUpperCase()).join('') : 'BW'
export default function AppLayout({ children, breadcrumb = 'Workspace / Overview', activeItem = 'Dashboard', onNavigate, user, onLogout }) {
  const displayName = user?.fullName || 'Jordan Doe'
  const displayRole = user?.roles?.[0] || 'Team member'
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand__mark">BW</div><div><div className="brand__name">BuildWise</div><div className="brand__tagline">Project operations</div></div></div><nav className="nav" aria-label="Main navigation">{navigation.map((item) => <button key={item} type="button" className={`nav-item ${item === activeItem ? 'nav-item--active' : ''}`} onClick={() => onNavigate?.(item)}>{item}</button>)}</nav></aside><div className="app-main"><header className="topbar"><div><span className="mobile-brand">BuildWise</span><span className="breadcrumb">{breadcrumb}</span></div><div className="topbar__actions"><button className="icon-button" aria-label="Notifications">♢</button><div className="user"><div className="avatar">{initialsOf(displayName)}</div><div className="user__meta"><div className="user__name">{displayName}</div><div className="user__role">{displayRole}</div></div></div>{onLogout && <button type="button" className="icon-button" aria-label="Sign out" title="Sign out" onClick={onLogout}>⏻</button>}</div></header><main className="content">{children}</main></div></div>
}
